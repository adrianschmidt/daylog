# Claude Instructions

## Pull Requests

**CRITICAL: ALL pull requests MUST target the `main` branch of `adrianschmidt/daylog`.** Never open PRs against the upstream `tfolkman/daylog` repository unless explicitly instructed otherwise.

This is a fork. The remote origin should be `https://github.com/adrianschmidt/daylog.git`.
